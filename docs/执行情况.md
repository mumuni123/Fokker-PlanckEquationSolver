# 阶段：`F1` 执行报告

## 当前阶段
`F1`

## 实际修改文件
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_energy_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容
1. 在 `initialize_maxwellian(0.0)` 后调用 `electrons.compute_moments()`，再复制 `electrons.number_density` 为固定离子密度，修复 F1.1 的初始化顺序。
2. 增加 F1.2 所需的全局初始化诊断：`electron_density_max/min`、`ion_density_max/min`、`initial_rho_linf`、`initial_E_linf`、`initial_gauss_linf`；未修改联合中点生产算子。

## 编译命令
```
make -j4 joint_phase_space_midpoint_energy_test

集群编译/运行结果已由远程只读审计取得；本机 PowerShell 未执行该 Linux make 命令。
```

## 测试命令
```
ssh XY_server 后，在集群项目目录执行的读取命令：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,240p' output/joint_midpoint_j1_unit.result
```

数值结果：
```
status=PASS
accepted=1
finite=1
gauss_ok=1
converged=1
iterations=1
residual_linf=0
poisson_residual_linf=0
energy_residual=0
failure_code=0
electron_density_max=1.1999999999999988e+29
electron_density_min=1.1999999999999988e+29
ion_density_max=1.1999999999999988e+29
ion_density_min=1.1999999999999988e+29
initial_rho_linf=0
initial_E_linf=0
initial_gauss_linf=0
历史错误值 1.1132815186046597e14 未出现在本次结果中；按 F1.3 标记为历史错误测试结果，不能作为纯联合离散能量误差。
```

阶段门：
PASS

- F1.3 的 `smooth-background` 结果满足文档要求：接受、有限、Gauss 通过、中点收敛、残差与能量误差为零、失败码为零。
- 下一步允许进入 `F2`，但本报告不自动执行 F2。

---

# 阶段：`F2` 执行报告

## 当前阶段
`F2`

## 实际修改文件
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_energy_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容
1. 已按 F2.1 增加 `smooth-perturbed-background`，并通过集群结果确认该 case 被实际运行。
2. 未修改生产联合算子、Poisson、Beam、Tail、碰撞模型或任何阈值。

## 编译命令
```
make -j4 joint_phase_space_midpoint_energy_test
```

## 测试命令
```
ssh XY_server 后只读集群结果：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,260p' output/f2_smooth_perturbed_background.result
```

数值结果：
```
status=FAIL
case=smooth-perturbed-background
accepted=0
finite=1
gauss_ok=1
converged=1
iterations=1
residual_linf=9.2918052521601289e-11
poisson_residual_linf=2.0841129930082554e-16
energy_residual=-6.267985814710385e-22
failure_code=75
failure_stage=joint_midpoint_energy_residual
electron_density_max=1.2000848528137418e+29
electron_density_min=1.1999151471862572e+29
ion_density_max=1.2e+29
ion_density_min=1.2e+29
initial_rho_linf=1359494.8112243628
initial_E_linf=153542538755.59061
initial_gauss_linf=32
```

阶段门：
PASS

- `smooth-perturbed-background` 本身已经按 F2.1 正确建立：电子密度相对固定离子密度具有约 `1e-4` 的余弦扰动，且 `initial_E_linf=1.5354253875559061e11` 非零。因此失败**不是** F2 的 profile 初始化失败，也不是 Poisson 未收敛；`finite=1`、`gauss_ok=1`、`converged=1` 也证明该夹具已经进入有效的非平凡场场景。
- 首个拒绝点是 `src/vpfp_integrator.cpp:3661-3665`。该处以 `abs(combined_energy_residual) / candidate_energy_scale > 1e-8` 触发 `failure_code=75`，其中 `combined_energy_residual = delta_ke_u + field_energy_change - electrode_work`。
- 报告中的 `energy_residual=-6.267985814710385e-22 J/m²` 是合并残差的**绝对值**；`code 75` 作为联合步状态诊断原样保留，不被改写或隐藏。
- F2 的阶段目标是新增并运行非零场测试夹具，而不是在本阶段修复 F3--F8 所定义的离散能量根因。该目标已完成；后续离散修复必须仍严格按 F3 顺序进行。
- F2 已通过，允许进入 F3；本次不自动执行 F3。

---

# 阶段：`F3` 执行报告

## 当前阶段

`F3`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\joint_phase_space_midpoint.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_unit_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. F3.1/F3.2 已移除两处 u 通量中的重复 `sg.dx * vg.uperp_ring_areas[k]`，生产表达式为 `a * f_trace`。
2. 已加入直接调用生产 `build_periodic_center_flux()` 的 `dx/ring` 几何审计，并修复其测试辅助函数声明顺序。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_unit_test
```

## 测试命令
```text
ssh XY_server 后只读集群结果：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,320p' output/f3_joint_midpoint_unit.result
sed -n '1,220p' output/f3_joint_midpoint_unit.out
sed -n '1,220p' output/f3_joint_midpoint_unit.err
```

数值结果：
```text
status=PASS
case_positive_field_pass=1
case_negative_field_pass=1
case_single_velocity_pass=1
case_two_velocity_pass=1
u_flux_geometry_selected=1
u_flux_geometry_pass=1
u_flux_geometry_dx1=9.9999999999999995e-07
u_flux_geometry_dx2=1.9999999999999999e-06
u_flux_geometry_ring_k1=0.00077228931308974684
u_flux_geometry_ring_k2=0.0034144742062359136
u_flux_geometry_relative_error_dx1=1.5428587986365677e-16
u_flux_geometry_relative_error_dx2=0
mass_residual=882.84375
kinetic_work_residual=1.1223186551119479e-20
poisson_work_residual=2.4074124304840448e-35
combined_energy_residual=1.1223186551119479e-20
g_gstar_residual=4.7866129965483547e-35
u_boundary_flux=0
err=空
```

阶段门：
PASS

- `joint_phase_space_midpoint_unit_test` 的原有场/速度场景全部通过。
- 新 u-flux geometry test 的两个 `dx`/ring 组合均达到浮点舍入量级，满足 F3.4/F3.5。
- 允许进入 F4；本次不自动执行 F4。

---

# 阶段：`F4` 执行报告

## 当前阶段

`F4`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\joint_phase_space_midpoint.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_unit_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 在 `evaluate_local_residual()` 中构造并 finite-check `m_mid_local=0.5*(m_old_local+m_candidate_local)`；MPI ghost、x trace 和 u trace 全部改用 midpoint。
2. 增加单 rank global/local residual 一致性测试，比较 `x_flux_rate`、`u_flux_rate`、三个 mass delta 和 residual；candidate 仅用于 residual 左侧与非负性检查。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_unit_test
```

## 测试命令
```text
ssh XY_server 后只读集群结果：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,360p' output/f4_joint_midpoint_unit.result
sed -n '1,240p' output/f4_joint_midpoint_unit.out
sed -n '1,240p' output/f4_joint_midpoint_unit.err
```

数值结果：
```text
status=PASS
case_positive_field_local_global_flux_pass=1
case_positive_field_local_global_flux_relative_error=0
case_negative_field_local_global_flux_pass=1
case_negative_field_local_global_flux_relative_error=0
case_single_velocity_local_global_flux_pass=1
case_single_velocity_local_global_flux_relative_error=0
case_two_velocity_local_global_flux_pass=1
case_two_velocity_local_global_flux_relative_error=0
u_flux_geometry_pass=1
u_flux_geometry_relative_error_dx1=1.5428587986365677e-16
u_flux_geometry_relative_error_dx2=0
case_positive_field_pass=1
case_negative_field_pass=1
case_single_velocity_pass=1
case_two_velocity_pass=1
err=空
```

阶段门：
PASS

- F4.6 的 global/local `x_flux_rate`、`u_flux_rate`、`mass_delta_x`、`mass_delta_u`、`mass_delta_total` 和 residual 均在 roundoff 内一致；四个 case 的相对误差均为 `0`。
- F4 已通过，允许进入 F5；本次不自动执行 F5。

---

# 阶段：`F5` 执行报告

## 当前阶段

`F5`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\joint_phase_space_midpoint.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_unit_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 将 `build_hamiltonian_velocity()` 改为文档规定的 u-face 到 cell 的严格 transpose，并加入 F5.2 要求的禁止替换说明；未保留 centered derivative 生产分支。
2. 增加独立 u-energy adjoint 测试：左侧使用实际 `u_flux_rate`，右侧独立使用 `vH` 与正的、依赖 `ix/j/k` 的 `M_mid`，同时检查非均匀网格、反对称性、finite 和光速上限。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_unit_test
```

## 测试命令
```text
ssh XY_server 后只读集群结果：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,420p' output/f5_joint_midpoint_unit.result
sed -n '1,260p' output/f5_joint_midpoint_unit.out
sed -n '1,260p' output/f5_joint_midpoint_unit.err
```

数值结果：
```text
status=PASS
case_positive_field_pass=1
case_negative_field_pass=1
case_single_velocity_pass=1
case_two_velocity_pass=1
case_positive_field_u_energy_adjoint_pass=1
case_negative_field_u_energy_adjoint_pass=1
case_single_velocity_u_energy_adjoint_pass=1
case_two_velocity_u_energy_adjoint_pass=1
case_positive_field_u_energy_adjoint_relative_error=1.9965931722903202e-16
case_negative_field_u_energy_adjoint_relative_error=1.9965931722903202e-16
case_single_velocity_u_energy_adjoint_relative_error=5.9600740763580538e-17
case_two_velocity_u_energy_adjoint_relative_error=0
case_positive_field_u_energy_velocity_symmetry_error=0
case_negative_field_u_energy_velocity_symmetry_error=0
case_single_velocity_u_energy_velocity_symmetry_error=0
case_two_velocity_u_energy_velocity_symmetry_error=0
u_energy_velocity_max_abs=207062768.99175298
u_flux_geometry_pass=1
u_flux_geometry_relative_error_dx1=1.5428587986365677e-16
u_flux_geometry_relative_error_dx2=0
err=空
```

阶段门：
PASS

- F5.3 的实际 u-flux work 与独立 `vH*M_mid` work 对偶误差均为约 `2e-16` 或更小。
- F5.4 使用生产非均匀 `CylindricalVelocityGrid`；F5.5 的速度均 finite、反对称误差为 `0`，最大绝对速度 `2.0706276899175298e8 m/s` 小于 `c`，未使用 clip。
- F5 已通过，允许进入 F6；本次不自动执行 F6。

---

# 阶段：`F6` 执行报告

## 当前阶段

`F6`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\vpfp_integrator.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\vpfp_integrator.h`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_energy_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 在 J1 candidate evaluate 的 Poisson solve 后调用现有 `build_potential_pairing_field(fields, eval_fields, ...)`，其中 `fields` 为接受态、`eval_fields` 为 candidate；构造失败直接使 candidate evaluation 失败。
2. 使用相邻 pairing face 平均构造 `e_pair_cell`，并将其传给 `evaluate_local_residual()`；移除了 `eval_fields.Ex` 作为 J1 force field 的路径，未添加 fallback。
3. 扩展 `VpfpStepResult` 与 J1 测试：生产 candidate evaluate 真实记录 `joint_midpoint_pairing_field_built`，测试通过 `integrator.advance()` 读取该状态并纳入验收；未在测试中复写 pairing 公式。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_energy_test
```

## 测试命令
```text
ssh XY_server 后只读集群结果；该结果必须由 `joint_phase_space_midpoint_energy_test` 生成：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
./build/joint_phase_space_midpoint_energy_test --case smooth-background --result output/f6_joint_midpoint_unit.result
sed -n '1,420p' output/f6_joint_midpoint_unit.result
sed -n '1,260p' output/f6_joint_midpoint_unit.out
sed -n '1,260p' output/f6_joint_midpoint_unit.err
```

数值结果：
```text
status=PASS
case=smooth-background
accepted=1
finite=1
gauss_ok=1
converged=1
production_advance_called=1
joint_midpoint_pairing_field_built=1
iterations=1
residual_linf=0
poisson_residual_linf=0
energy_residual=0
failure_code=0
pairing_field_runtime=PASS
pairing_field_fallback=0
err=空
```

阶段门：
PASS

- F6.2–F6.5 已由生产 `integrator.advance()` 实际执行并通过：candidate pairing field 构造成功，未发生 fallback，J1 residual 使用 pairing cell field。
- `accepted=1`、`finite=1`、`gauss_ok=1`、`converged=1`、`failure_code=0`，满足 F6 阶段门。
- F6 已通过，允许进入 F7；本次不自动执行 F7。

---

# 阶段：`F7` 执行报告

## 当前阶段
`F7`

## 实际修改文件
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\vpfp_integrator.h`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\src\vpfp_integrator.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_energy_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\VPFP_能量根因链最终核查与逐阶段修复实施方案.md`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容
1. 在生产 J1 最终 candidate 上输出 F7.1 要求的 `joint_midpoint_delta_k_x`、`joint_midpoint_delta_k_u`、`joint_midpoint_u_face_work`、`joint_midpoint_charge_current_work`、`joint_midpoint_poisson_potential_charge_work`、`joint_midpoint_poisson_transport_residual`、`joint_midpoint_current_pair_residual`、`joint_midpoint_domain_energy_change` 和总能量残差。
2. `W_u` 直接来自 `accepted_bundle.u_flux_rate`；`W_J` 直接来自 `accepted_bundle.charge_current_face` 与最终 pairing face；`R_PJ` 复用 `evaluate_work_identity()` 的场能/电极功。face 积分采用物理 endpoint 半权重与 MPI shared-face 单次归属。未修改 Poisson、阈值或物理推进。
3. 在读取本次 F7 结果后，补充 `joint_midpoint_force_current_work`、`joint_midpoint_charge_current_work_interior`、`joint_midpoint_charge_current_work_endpoint` 与 `joint_midpoint_force_charge_residual`。这些字段只用于区分内部 x/u 配对和周期接缝/物理 endpoint 拓扑余额；尚待下一次 F7 重跑产生数值。

## 编译命令
```
make -j4 joint_phase_space_midpoint_energy_test
```

## 测试命令
```
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver

yhrun -n 1 --cpu-bind=cores stdbuf -oL -eL \
  ./build/joint_phase_space_midpoint_energy_test \
  --case smooth-perturbed-background \
  --result ./output/f7_smooth_perturbed_background.result \
  > ./output/f7_smooth_perturbed_background.out \
  2> ./output/f7_smooth_perturbed_background.err
```

数值结果：
```
status=FAIL
accepted=0
finite=1
gauss_ok=1
converged=1
production_advance_called=1
joint_midpoint_pairing_field_built=1
joint_midpoint_delta_k_x=1.687096535559801e-17
joint_midpoint_delta_k_u=28.891863089342269
joint_midpoint_u_face_work=28.891863089349869
joint_midpoint_charge_current_work=22.224220061661196
joint_midpoint_poisson_potential_charge_work=-22.224225356595088
joint_midpoint_poisson_transport_residual=-5.2950189122213942e-06
joint_midpoint_current_pair_residual=6.6676430276886727
joint_midpoint_domain_energy_change=6.6676377326621612
energy_residual=6.6676377326697605
min_mass=-0.19890667229868997
max_mass=3.9209416160861854e+21
iterations=3
residual_linf=1.1480138226714858e-13
poisson_residual_linf=2.084279431871553e-16
failure_code=75
failure_stage=joint_midpoint_energy_residual

joint_midpoint_force_current_work=28.891863089349886
joint_midpoint_charge_current_work_interior=22.224220061661196
joint_midpoint_charge_current_work_endpoint=-1.2319557390016078e-21
joint_midpoint_force_charge_residual=6.6676430276886904
```

阶段门：
PASS（F7 诊断实现）

- F7 的阶段目标是建立并输出 x/u/Poisson 三重功诊断，而不是在本阶段通过 J1 的最终能量 gate。`production_advance_called=1`、`joint_midpoint_pairing_field_built=1` 及上述完整字段证明诊断已由生产 J1 路径实际生成。
- `status=FAIL`、`failure_code=75` 表示 **J1 非平凡场能量闭合仍失败**，不是 F7 诊断功能失败。主要余额为 `R_uJ=6.6676430276886727 J/m2`，而 `R_PJ=-5.2950189122213942e-06 J/m2` 很小。
- 新增分解确认 `W_u-W_F=-1.7e-14 J/m2`，所以 u trace、`vH` 与 accepted midpoint mass 的 u-work 配对已闭合；但 `W_F-W_J=6.6676430276886904 J/m2`。直接失败对象已收窄为 x-face `charge_current_face` 与 cell-force/face-pairing 映射。`W_J_endpoint` 本身近零，但这尚不能单独排除周期接缝对首末 cell force-work 的影响；需要在第 11 节情形 B 的允许范围内继续分解 x flux 与 boundary-cell force work。
- 不得将 `status=FAIL` 伪装为 J1 PASS，不得放宽 code 75，不得在 F7 中缩放或重构 `J_charge`。在新增分解结果出来前，不进入改变 x 边界拓扑或 J2 接入顺序的实现阶段。

---

# 阶段：`F8` 执行报告

## 当前阶段

`F8`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 复核 `advance_joint_midpoint()`：没有发现 `result.joint_midpoint_energy_residual = result.ledger.domain_energy_change` 的 commit 后覆盖。
2. 确认 `joint_midpoint_energy_residual` 独立表示 `R_E`，`joint_midpoint_domain_energy_change` 独立表示域能量变化；本阶段没有代码修改。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_energy_test
```

## 测试命令
```text
源码静态复核；F8 无新增数值算法测试。
```

数值结果：
```text
源码语义复核=PASS
joint_midpoint_energy_residual_overwrite=不存在
joint_midpoint_domain_energy_change=独立字段
编译=未执行（F8 仅要求字段覆盖语义复核）
```

阶段门：
PASS

- F8.1 的源码语义复核通过，未发现 commit 后覆盖。
- F8 已完成，允许进入 F9；本次不自动执行 F9。

---

# 阶段：`F9` 执行报告

## 当前阶段

`F9`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\tests\joint_phase_space_midpoint_unit_test.cpp`
- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 补充 J0-A cell-mass 单位诊断，并将 `M=f*dx*du*A_perp` 的相对误差纳入 geometry 审计。
2. 补充 J0-E 内部 x-face `J_charge=(J_force_left+J_force_right)/2` 的独立生产 bundle 对偶测试，并将其纳入总体 PASS；J0-B/C/D/F 保持现有直接生产实现测试。

## 编译命令
```text
make -j4 joint_phase_space_midpoint_unit_test
```

## 测试命令
```text
ssh XY_server 后只读集群结果：
cd /HOME/sztu_lbju/sztu_lbju_1/HDD_POOL/Chendong/FokkerPlanckEquationSolver
sed -n '1,520p' output/f9_j0_all.result
sed -n '1,300p' output/f9_j0_all.out
sed -n '1,300p' output/f9_j0_all.err
```

数值结果：
```text
status=PASS
j0_a_cell_mass_pass=1
j0_b_u_flux_geometry_pass=1
j0_c_midpoint_consistency_pass=1
j0_d_u_work_force_adjoint_pass=1
j0_e_x_current_force_adjoint_pass=1
j0_f_poisson_pairing_pass=1
j0_all_six_pass=1
u_flux_geometry_cell_mass_relative_error=0
u_flux_geometry_relative_error_dx1=1.5428587986365677e-16
u_flux_geometry_relative_error_dx2=0
case_positive_field_x_force_charge_adjoint_relative_error=8.7514673085040703e-15
case_negative_field_x_force_charge_adjoint_relative_error=8.7514673085040703e-15
case_single_velocity_x_force_charge_adjoint_relative_error=1.3902224228911254e-16
case_two_velocity_x_force_charge_adjoint_relative_error=0
case_positive_field_u_energy_adjoint_relative_error=1.9965931722903202e-16
case_single_velocity_u_energy_adjoint_relative_error=5.9600740763580538e-17
err=空
```

阶段门：
PASS

- J0-A 至 J0-F 六项均为 `1`，`j0_all_six_pass=1`。
- cell mass、u-flux geometry、midpoint 一致性、u-work adjoint、x-current/force-current adjoint 和 Poisson pairing 均满足各自 roundoff/既定容差。
- F9 已通过，允许进入 F10。
- F10 集群结果已产生并单独记录如下；两档必须同时通过，当前不能进入 J1 MPI 门。

---

# 阶段：`F10` 执行报告

## 当前阶段

`F10`

## 实际修改文件

- `E:\ScientificComputation\Fokker-PlanckEquationSolver\docs\执行情况.md`

## 修改内容

1. 按 F10.1 和 §12 对 `smooth-background`、`smooth-perturbed-background` 两个单 rank J1 结果进行逐项登记。
2. 未修改生产算法、阈值或测试程序。

## 编译命令
```text
cmake --build build --target joint_phase_space_midpoint_energy_test -j4
```

## 测试命令
```text
yhrun -N 1 -n 1 --cpu-bind=cores ./build/joint_phase_space_midpoint_energy_test \
  --case smooth-background \
  --result ./output/j1_smooth-background_n1.result

yhrun -N 1 -n 1 --cpu-bind=cores ./build/joint_phase_space_midpoint_energy_test \
  --case smooth-perturbed-background \
  --result ./output/j1_smooth-perturbed-background_n1.result
```

数值结果：
```text
smooth-background:
status=PASS
accepted=1
finite=1
gauss_ok=1
converged=1
production_advance_called=1
joint_midpoint_pairing_field_built=1
residual_linf=0
poisson_residual_linf=0
energy_residual=0
failure_code=0

smooth-perturbed-background:
status=FAIL
accepted=0
finite=1
gauss_ok=1
converged=1
production_advance_called=1
joint_midpoint_pairing_field_built=1
joint_midpoint_delta_k_x=1.687096535559801e-17
joint_midpoint_delta_k_u=28.891863089342269
joint_midpoint_u_face_work=28.891863089349869
joint_midpoint_force_current_work=28.891863089349886
joint_midpoint_charge_current_work=22.224220061661196
joint_midpoint_charge_current_work_interior=22.224220061661196
joint_midpoint_charge_current_work_endpoint=-1.2319557390016078e-21
joint_midpoint_poisson_transport_residual=-5.2950189122213942e-06
joint_midpoint_current_pair_residual=6.6676430276886727
joint_midpoint_force_charge_residual=6.6676430276886904
joint_midpoint_domain_energy_change=6.6676377326621612
energy_residual=6.6676377326697605
min_mass=-0.19890667229868997
max_mass=3.9209416160861854e+21
iterations=3
residual_linf=1.1480138226714858e-13
poisson_residual_linf=2.084279431871553e-16
failure_code=75
failure_stage=joint_midpoint_energy_residual
```

阶段门：
FAIL

- `smooth-background` 满足 F10 条件，但它只是零场平衡态，不替代非零场验收。
- `smooth-perturbed-background` 虽然 finite、Gauss、Newton residual 和 pairing field 均正常，但 `accepted=0`、`failure_code=75`。因此 F10 的 J1 非平凡场能量闭合失败。
- 严格按主方案第 11 节选择**情形 B**：`R_uJ=6.6676430276886727 J/m2` 显著，而 `R_PJ=-5.2950189122213942e-06 J/m2` 相对主残差小六个数量级。情形 A 不成立；情形 C 不成立，因为 `R_uJ` 不小；情形 D/F11 不适用，因为能量恒等式尚未通过且失败码为 `75`，不是 `73/74/76`。
- `W_u=28.891863089349869 J/m2` 与同源 cell-force 功 `W_F=28.891863089349886 J/m2` 的差约 `1.7e-14 J/m2`，排除 u trace、`vH` 和 midpoint mass 的 u-work 配对作为当前主因。主残差出现在 `W_F-W_J=6.6676430276886904 J/m2`，即 x flux 形成的 `charge_current_face`、face averaging/ownership 或它与 pairing face 的映射未构成同一离散功对象。
- 允许的后续检查范围仅为情形 B：生产 x flux、`charge_current_face`、face averaging、midpoint mass、boundary-cell/periodic-seam 映射和 MPI face ownership。不得修改 Newton、Poisson；不得进入 F11、J1 MPI、J2 或 J3。
